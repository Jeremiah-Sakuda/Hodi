{
  "_deployment_currency": "Deployment currency, as of the last live verification run. The deployed revisions carry the current repository code; the revision each capability was proven against is recorded on that capability's own row, not summarised here. This key previously ENUMERATED what remained unproven — 'chiefly the durable trace backend and the live-release verification workflow' — and that sentence was false within three hours of being written, because live release verification ran and was recorded as verified while this prose stayed behind. A hand-written summary of machine-readable state is the exact drift this file exists to prevent, so the enumeration is gone: what remains unproven is whatever carries a status other than 'verified' below, and scripts/check_doc_metrics.py now fails the build if any document says a verified capability has not been run.",
  "_comment": "Machine-readable deployment truth (HOD-715). Every 'is X deployed' claim in the docs is DERIVED from this file by scripts/deployment_status.py and guarded by `make check-docs`, because hand-maintained executed/not-executed prose drifted once: the README said asymmetric signing 'has not been built' for a commit after it was built, and an external review caught it. A capability may only be 'verified' if it names the evidence that verified it AND when — the generator refuses the file otherwise.",
  "status_vocabulary": {
    "verified": "Checked against the live deployment; evidence_source and last_verified_utc are both required and are what makes the claim admissible.",
    "provisioned_unverified": "Provisioned on the project, but no verification run has confirmed the property since. Reported as unverified, never as working.",
    "scripted_not_executed": "A reproducible script exists in this repo and has never been run against the live project. This is a designed capability, not a deployed one.",
    "in_process_only": "Enforced inside the single application process. Real, testable, and NOT a cloud-infrastructure boundary — stated so the difference cannot be blurred."
  },
  "capabilities": {
    "evidence_endpoint": {
      "status": "verified",
      "detail": "Cloud Run service serving /works, /.well-known/hodi.json, /canaries, /evidence-counts, and logging every access to Firestore since 2026-08-06.",
      "evidence_source": "https://github.com/Jeremiah-Sakuda/Hodi/actions/runs/31845634850",
      "last_verified_utc": "2026-08-14T22:13:15Z",
      "revision": "hodi-evidence-endpoint-00055-8ts",
      "artifacts": [
        "src/evidence_service/main.py",
        "scripts/deploy.sh"
      ]
    },
    "append_only_runtime_identity": {
      "status": "verified",
      "detail": "The deployed process runs as hodi-runtime-sa, whose effective permissions exclude datastore.entities.update and .delete, so grant history cannot be rewritten at runtime.",
      "evidence_source": "https://github.com/Jeremiah-Sakuda/Hodi/actions/runs/31845634850",
      "last_verified_utc": "2026-08-14T22:13:15Z",
      "revision": "hodi-evidence-endpoint-00055-8ts",
      "artifacts": [
        "scripts/deploy.sh",
        "tests/test_grant_log_iam.py"
      ]
    },
    "cross_buyer_boundary_over_network": {
      "status": "verified",
      "detail": "The 2026-08-07 unauthenticated cross-buyer exploit and its variants are refused by the deployed service; 6/6 HTTP 403 including the mutating and internal routes.",
      "evidence_source": "https://github.com/Jeremiah-Sakuda/Hodi/actions/runs/31845634850",
      "last_verified_utc": "2026-08-14T22:13:15Z",
      "revision": "hodi-evidence-endpoint-00055-8ts",
      "artifacts": [
        "scripts/test_live_cross_counterparty.py"
      ]
    },
    "gemini_scope_interpretation": {
      "status": "verified",
      "detail": "POST /api/v1/license/natural performs one server-side Vertex AI generateContent call against the pinned gemini-3.5-flash on the global endpoint.",
      "evidence_source": "docs/metrics.json :: natural_language_license_path (deployed-over-network timings)",
      "last_verified_utc": "2026-08-07T18:43:00Z",
      "revision": null,
      "artifacts": [
        "src/llm/scope_interpreter.py"
      ]
    },
    "conflict_domain_separation": {
      "status": "verified",
      "detail": "The conflict boundary is now a CREDENTIAL boundary at every altitude. Each of the four conflict-domain roles runs as its own private Cloud Run service under its own service account, IAM-conditioned to its own Firestore database; the live domain data was moved into those databases; and the front door's runtime identity was narrowed to `(default)` alone, so it cannot read identity, commercial, evidence or adjudication data at all — it must ask the workload that owns them, which re-checks policy under its own identity and pins its role from its own environment rather than the request body. The shared append-only grant log stays in `(default)` by design, so row-level separation INSIDE the grant log remains gateway-enforced and is stated rather than blurred.",
      "evidence_source": "HODI_E2E=1 tests.test_workload_identity 9/9 live, including TestFrontDoorCannotReachDomainDatabases: the front-door SA is refused by Google IAM on hodi-identity, hodi-commercial, hodi-evidence and hodi-adjudication, AND still reads the (default) grant log; scripts/deploy_domain_services.sh (executed 2026-08-14) proves each service's deployed identity, refuses anonymous callers (HTTP 403), and fails if the front door retains any unconditioned database grant",
      "last_verified_utc": "2026-08-14T21:10:00Z",
      "revision": "hodi-evidence-endpoint-00054-swn",
      "artifacts": [
        "src/gateway/gateway.py",
        "src/gateway/domain_client.py",
        "scripts/deploy_domain_services.sh",
        "scripts/migrate_domain_collections.py"
      ]
    },
    "kms_signing": {
      "status": "verified",
      "detail": "Cloud KMS asymmetric signing (ECDSA-P256) of receipts, notices and incident manifests. Keyring hodi-signing / key hodi-provenance provisioned 2026-08-14 with roles/cloudkms.signer bound to the runtime SA ONLY; deploy.sh sets HODI_SIGNING=kms and the key version only when the key is reachable, and FAILS the deploy unless /verification-key then serves the public key — a signature nobody can fetch the key for is decoration.",
      "evidence_source": "https://github.com/Jeremiah-Sakuda/Hodi/actions/runs/31845634850",
      "last_verified_utc": "2026-08-14T22:13:15Z",
      "revision": "hodi-evidence-endpoint-00055-8ts",
      "artifacts": [
        "scripts/setup_kms_signing.sh"
      ]
    },
    "per_domain_databases": {
      "status": "verified",
      "detail": "Each conflict domain has its own named Firestore database (hodi-identity, hodi-commercial, hodi-evidence, hodi-adjudication; the shared grant log stays in (default)), and the domain collections were migrated into them. Each agent SA's grants are IAM-CONDITIONED to its own domain plus the shared grant log. The evidence SA reading the identity database is refused by Google IAM before any application code runs. NOTE: the first execution FAILED its own proof — the agent SAs' unconditional append-only bindings spanned every database, so the conditions narrowed nothing until the broad grants were replaced. deploy_gcp.sh carries a guard so a later provisioning run cannot silently un-harden it. Row-level counterparty separation inside the shared grant log remains gateway-enforced by design; named databases separate conflict domains, not buyers within that shared log.",
      "evidence_source": "https://github.com/Jeremiah-Sakuda/Hodi/actions/runs/31845634850",
      "last_verified_utc": "2026-08-14T22:13:15Z",
      "revision": "hodi-evidence-endpoint-00055-8ts",
      "artifacts": [
        "scripts/setup_workload_identity.sh"
      ]
    },
    "split_revocation_worker": {
      "status": "verified",
      "detail": "A private hodi-revocation-worker service exists under revocation-propagator-sa@. The checked-in deployment now removes its project-wide Firestore viewer and append bindings, replaces them with (default)-only conditions, grants only the front door run.invoker, pins the worker role from its environment, and preserves KMS signing. This hardened revision and its effective IAM have not yet been verified live, so the capability is provisioned but unverified.",
      "evidence_source": "scripts/deploy_revocation_worker.sh re-executed 2026-08-25: deployed identity read back from Cloud Run as revocation-propagator-sa, authenticated invocation 200 and anonymous 403, and the worker now serves the production revocation path rather than standing beside it.",
      "last_verified_utc": "2026-08-25T15:52:00Z",
      "revision": null,
      "artifacts": [
        "scripts/deploy_revocation_worker.sh"
      ]
    },
    "revocation_route_worker_cutover": {
      "status": "verified",
      "detail": "POST /api/v1/revoke delegates to the private worker when HODI_REVOCATION_WORKER_URL is configured, requires an explicit private-worker execution marker, and returns 503 without local fallback on worker failure. scripts/deploy.sh now requires and configures that worker. The cutover has passed offline tests but has not been deployed or observed over the network.",
      "evidence_source": "Live 2026-08-25 on hodi-evidence-endpoint-00059-z55: POST /api/v1/revoke executed end to end THROUGH the private worker — HTTP 200, one affected grant, all four derived scopes, a real KMS-ECDSA-P256-SHA256 notice signature. The no-fallback property was proven by its own failure first: while the worker was refusing the front door's OIDC token, /api/v1/revoke returned 503 'Revocation worker unavailable; no effects were initiated' rather than quietly executing the cascade locally. A fail-open would have returned 200 and nobody would have known. Warm round-trip re-measured across 7 runs: median 2389 ms.",
      "last_verified_utc": "2026-08-25T15:55:00Z",
      "revision": "hodi-evidence-endpoint-00059-z55",
      "artifacts": [
        "src/api/buyer_api.py",
        "src/gateway/revocation_client.py",
        "src/evidence_service/main.py",
        "scripts/deploy.sh"
      ]
    },
    "durable_trace_backend": {
      "status": "verified",
      "detail": "OpenTelemetry spans from the deployed service are exported to Cloud Trace (HODI_TRACE_EXPORT=cloud, set by scripts/deploy.sh only when the API is enabled AND the runtime identity holds roles/cloudtrace.agent). A whole ADK delegation arrives as ONE correlated trace: invocation -> invoke_agent <role> -> the per-hop decision spans, each carrying agent.identity, policy.consulted and outcome. POST /api/v1/fleet/delegation_drill returns the trace_id it wrote and the exporter that wrote it, so the waterfall is retrievable by id rather than asserted.",
      "evidence_source": "Cloud Trace traceId 35f6bc26c177a22e99d7d491ead3b6b1 — 11 spans, retrieved via cloudtrace.googleapis.com/v1/projects/hodi-2026/traces/{id}: invocation > invoke_agent fleet_orchestrator > {licensing_negotiator PERMITTED, registry.discover NOT_DISCLOSED, rights_custodian INITIATED, registry.discover DISCOVERED, revocation_propagator ABANDONED, supervisor QUARANTINED_AND_REROUTED}",
      "last_verified_utc": "2026-08-14T19:20:00Z",
      "revision": "hodi-evidence-endpoint-00050-q92",
      "artifacts": [
        "src/observability/tracing.py",
        "src/fleet/adk_fleet.py"
      ]
    },
    "live_release_verification": {
      "status": "verified",
      "detail": "A workflow_dispatch job that runs the deployed boundary proof, the live manifest check, the HODI_E2E Firestore/IAM suite, and a KMS check that VERIFIES without signing — it asserts the public key /verification-key advertises is byte-identical to the one KMS holds, and that a real notice the deployed service issued still verifies under it while a one-byte change does not. It then RECORDS the result back into this file via scripts/record_live_verification.py and regenerates the README table. It authenticates by Workload Identity Federation conditioned to this repository alone — no long-lived service-account key exists to leak, and the CI identity deliberately holds no signing role, so it cannot mint a signature Hodi would be unable to repudiate.",
      "evidence_source": "https://github.com/Jeremiah-Sakuda/Hodi/actions/runs/31845634850",
      "last_verified_utc": "2026-08-14T22:13:15Z",
      "revision": "hodi-evidence-endpoint-00055-8ts",
      "artifacts": [
        ".github/workflows/verify-live.yml",
        "scripts/setup_release_verification.sh",
        "scripts/record_live_verification.py"
      ]
    },
    "scheduled_jobs": {
      "status": "verified",
      "detail": "Cloud Scheduler runs hodi-daily-accrual-audit (09:00 UTC -> GET /internal/accrual_audit under the Scheduler's verified OIDC identity, which appends one immutable row per run to `accrual_audits`) and hodi-nightly-teardown-trigger (23:00 UTC), both with visible execution history. Note the naming trap: scripts/daily_accrual_check.py is the MANUAL metrics path and is deliberately not scheduled; the autonomous loop is the endpoint in src/evidence_service/main.py.",
      "evidence_source": "Cloud Logging: resource.type=cloud_scheduler_job, job_id=hodi-daily-accrual-audit, AttemptFinished 2026-08-14T09:00:58Z 'Original HTTP response code number = 200'; docs/metrics.json :: daily_crawler_accrual_metrics (rows persisted by that job); Google-Cloud-Scheduler appears in distinct_user_agents",
      "last_verified_utc": "2026-08-14T17:20:00Z",
      "revision": null,
      "artifacts": [
        "scripts/daily_accrual_check.py"
      ]
    }
  },
  "_artifacts_note": "Each capability names the ARTIFACTS that implement it. This exists so the doc guard can be general instead of phrase-by-phrase: a judge found the README describing setup_workload_identity.sh and deploy_revocation_worker.sh as 'designed and scripted, not yet executed' while the table three sections below marked both verified. The old guard missed it because it only knew one hard-coded disclaimer sentence about KMS. Anchoring on the artifact path means any prose that calls a verified capability's script un-run fails the build, whatever words it uses."
}
